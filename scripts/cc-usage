#!/usr/bin/env bash
# cc-usage 全局入口 - 通过 python -m 调用 ccusage 包.
# 安装: 把本文件软链到 /usr/local/bin/cc-usage
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
exec python3 -m ccusage.cli "$@"
